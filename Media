local repo = 'https://raw.githubusercontent.com/violin-suzutsuki/LinoriaLib/main/'

local Library = loadstring(game:HttpGet(repo .. 'Library.lua'))()
local ThemeManager = loadstring(game:HttpGet(repo .. 'addons/ThemeManager.lua'))()
local SaveManager = loadstring(game:HttpGet(repo .. 'addons/SaveManager.lua'))()

-- Services
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- Create window
local Window = Library:CreateWindow({
    Title = 'Auralis | Beta Version',
    Center = true, 
    AutoShow = true,
})

-- Create Tabs
local Tabs = {
    Free = Window:AddTab ('Free'),
    Premium = Window:AddTab('Premium'),
    Beta = Window:AddTab('Beta'), 
}

-- Main Tab
local BetaLeftBox = Tabs.Beta:AddLeftGroupbox('Auralis Hub')
BetaLeftBox:AddLabel('Beta Verison: 1.0.0')
local PremiumLeftBox = Tabs.Premium:AddLeftGroupbox('Auralis Hub')
PremiumLeftBox:AddLabel('Premium Verison: 1.0.0')
local FreeLeftBox = Tabs.Free:AddLeftGroupbox('Auralis Hub')
FreeLeftBox:AddLabel('Free Verison: 1.0.0')

-- Define function separately
local function loadBetaLoneSurvivalScript()
    local scriptUrl = "https://raw.githubusercontent.com/SPSsalty/..../refs/heads/main/Beta"
    local success, result = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)

    if not success then
        warn("Error loading the script: " .. result)
    else
        print("Lone Survival script loaded successfully!")
    end

    -- Properly unload the UI
    Library:Unload()
end

local function loadBetaRNGDropperTycoonScript()
    local scriptUrl = "https://raw.githubusercontent.com/SPSsalty/..0../refs/heads/main/Beta"
    local success, result = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)

    if not success then
        warn("Error loading the script: " .. result)
    else
        print("RNG Dropper Tycoon script loaded successfully!")
    end

    -- Properly unload the UI
    Library:Unload()
end

local function loadBetaWorldMiningScript()
    local scriptUrl = "https://raw.githubusercontent.com/SPSsalty/..1../refs/heads/main/Beta"
    local success, result = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)

    if not success then
        warn("Error loading the script: " .. result)
    else
        print("Mining World script loaded successfully!")
    end

    -- Properly unload the UI
    Library:Unload()
end

local function loadBetaPlinkoTycoonScript()
    local scriptUrl = "https://raw.githubusercontent.com/SPSsalty/..2../refs/heads/main/Beta"
    local success, result = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)

    if not success then
        warn("Error loading the script: " .. result)
    else
        print("Plinko Tycoon script loaded successfully!")
    end

    -- Properly unload the UI
    Library:Unload()
end

local function loadPremiumLoneSurvivalScript()
    local scriptUrl = "https://raw.githubusercontent.com/SPSsalty/..../refs/heads/main/Paid"
    local success, result = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)

    if not success then
        warn("Error loading the script: " .. result)
    else
        print("Lone Survival script loaded successfully!")
    end

    -- Properly unload the UI
    Library:Unload()
end

local function loadPremiumRNGDropperTycoonScript()
    local scriptUrl = "https://raw.githubusercontent.com/SPSsalty/..0../refs/heads/main/Paid"
    local success, result = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)

    if not success then
        warn("Error loading the script: " .. result)
    else
        print("RNG Dropper Tycoon script loaded successfully!")
    end

    -- Properly unload the UI
    Library:Unload()
end

local function loadPremiumWorldMiningScript()
    local scriptUrl = "https://raw.githubusercontent.com/SPSsalty/..1../refs/heads/main/Paid"
    local success, result = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)

    if not success then
        warn("Error loading the script: " .. result)
    else
        print("Mining World script loaded successfully!")
    end

    -- Properly unload the UI
    Library:Unload()
end

local function loadFreeLoneSurvivalScript()
    local scriptUrl = "https://raw.githubusercontent.com/SPSsalty/..../refs/heads/main/Free"
    local success, result = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)

    if not success then
        warn("Error loading the script: " .. result)
    else
        print("Lone Survival script loaded successfully!")
    end

    -- Properly unload the UI
    Library:Unload()
end

local function loadFreeRNGDropperTycoonScript()
    local scriptUrl = "https://raw.githubusercontent.com/SPSsalty/..0../refs/heads/main/Free"
    local success, result = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)

    if not success then
        warn("Error loading the script: " .. result)
    else
        print("RNG Dropper Tycoon script loaded successfully!")
    end

    -- Properly unload the UI
    Library:Unload()
end

local function loadFreeWorldMiningScript()
    local scriptUrl = "https://raw.githubusercontent.com/SPSsalty/..1../refs/heads/main/Free"
    local success, result = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)

    if not success then
        warn("Error loading the script: " .. result)
    else
        print("Mining World script loaded successfully!")
    end

    -- Properly unload the UI
    Library:Unload()
end

BetaLeftBox:AddDivider()

BetaLeftBox:AddLabel('Beta Lone Script')

BetaLeftBox:AddButton({
    Text = 'Lone Survival',
    Func = loadBetaLoneSurvivalScript
})

BetaLeftBox:AddDivider()

BetaLeftBox:AddLabel('Beta RNG Dropper Tycoon Script')

BetaLeftBox:AddButton({
    Text = 'RNG Dropper Tycoon',
    Func = loadBetaRNGDropperTycoonScript
})

BetaLeftBox:AddDivider()

BetaLeftBox:AddLabel('Beta World Mining Script')

BetaLeftBox:AddButton({
    Text = 'World Mining Script',
    Func = loadBetaWorldMiningScript
})

BetaLeftBox:AddDivider()

BetaLeftBox:AddLabel('Beta Plinko Tycoon Script')

BetaLeftBox:AddButton({
    Text = 'Plinko Tycoon',
    Func = loadBetaPlinkoTycoonScript
})

PremiumLeftBox:AddDivider()

PremiumLeftBox:AddLabel('Premium Lone Script')

PremiumLeftBox:AddButton({
    Text = 'Lone Survival',
    Func = loadPremiumLoneSurvivalScript
})

PremiumLeftBox:AddDivider()

PremiumLeftBox:AddLabel('Premium RNG Dropper Tycoon Script')

PremiumLeftBox:AddButton({
    Text = 'RNG Dropper Tycoon',
    Func = loadPremiumRNGDropperTycoonScript
})

PremiumLeftBox:AddDivider()

PremiumLeftBox:AddLabel('Premium World Mining Script')

PremiumLeftBox:AddButton({
    Text = 'World Mining Script',
    Func = loadPremiumWorldMiningScript
})

FreeLeftBox:AddDivider()

FreeLeftBox:AddLabel('Free Lone Script')

FreeLeftBox:AddButton({
    Text = 'Lone Survival',
    Func = loadFreeLoneSurvivalScript
})

FreeLeftBox:AddDivider()

FreeLeftBox:AddLabel('Free RNG Dropper Tycoon Script')

FreeLeftBox:AddButton({
    Text = 'RNG Dropper Tycoon',
    Func = loadFreeRNGDropperTycoonScript
})

FreeLeftBox:AddDivider()

FreeLeftBox:AddLabel('Free World Mining Script')

FreeLeftBox:AddButton({
    Text = 'World Mining Script',
    Func = loadFreeWorldMiningScript
})
